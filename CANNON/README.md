# VR A-Frame starter

Files:
- `index.html` : main A-Frame scene
- `js/scene.js` : interaction helpers
- `serveur.py` : simple HTTP server

Run locally (Windows):
1. Open PowerShell in this folder `VR`.
2. Start server: `python ./serveur.py` or `python -m http.server 8000`
3. Note the printed URL and open it in the Quest browser (same Wi‑Fi, avoid eduroam).

Keep your deliverables in English for submission.
